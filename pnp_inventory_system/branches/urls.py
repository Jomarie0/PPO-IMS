from django.urls import path
from . import views

app_name = 'branches'

urlpatterns = [
    # Branch management views can be added here if needed
    # For now, branches are managed through Django admin
]
