from django.urls import path
from . import views

app_name = 'audit_logs'

urlpatterns = [
    # Audit log views can be added here if needed
    # For now, audit logs are managed through Django admin
]
